<?xml version="1.0" encoding="utf-8"?>
<layer-list xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- Lapisan Bayangan dengan Transparansi Tinggi -->
    <item>
        <shape android:shape="rectangle">
            <solid android:color="#22000000"/> <!-- Warna bayangan lebih transparan -->
            <corners android:radius="16dp"/>
            <corners android:radius="10dp"/>
            <padding
                android:left="5dp"
                android:top="5dp"
                android:right="5dp"
                android:bottom="5dp"/>
        </shape>
    </item>

    <!-- Color Layer -->
    <item>
        <shape android:shape="rectangle">
            <solid android:color="#B63E3E"/> <!-- Warna tambahan (merah) -->
            <corners android:radius="10dp"/>
        </shape>
    </item>

    <!-- White Background Layer -->
    <item>
        <shape android:shape="rectangle">
            <solid android:color="@android:color/transparent"/>
            <corners android:radius="45dp"/>
        </shape>
    </item>
</layer-list>
